package handlers;

import states.mainstates.PlayState;

class BrandonUnused {
    var brandon:Int = 504;

    function what() {
        PlayState.storyDifficulty = brandon;//faq u (crashes game)
    }
}